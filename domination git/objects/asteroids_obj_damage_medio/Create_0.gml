/// Vida da asteroids
vida = 2;

/// moveimento da asteroids
randomize();

rotac = random_range(1, 1.5);

speed = random_range(2.7, 3.1);

direction = irandom_range(0, 359);

//////

image_index = irandom_range(0, 2);